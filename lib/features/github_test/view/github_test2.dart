import 'package:flutter/material.dart';

class GithubTest2 extends StatelessWidget {
  const GithubTest2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
