import 'package:flutter/material.dart';

class GithubTest extends StatelessWidget {
  const GithubTest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text(""),
    );
  }
}
